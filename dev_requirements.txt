# git+https://github.com/supervisely/supervisely.git@some-test-branch

supervisely[aug,model-benchmark]==6.73.571

# RTX 5090 (Blackwell, compute capability 12.0) requires a CUDA 12.8-capable Torch stack.
--extra-index-url https://download.pytorch.org/whl/cu128

# OpenMMLab 3.x still relies on binary packages built against the NumPy 1.x ABI.
numpy<2
torch==2.7.1+cu128
torchvision==0.22.1+cu128
openmim==0.3.7
mmengine==0.10.7
# OpenMMLab does not publish cu128 wheels for mmcv, so docker/Dockerfile.tmpl builds this from source.
mmcv==2.1.0
mmdet==3.3.0
mmcls>=1.0.0rc0
yapf==0.40.1
